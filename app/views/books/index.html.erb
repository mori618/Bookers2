<% @books.each do |post_image| %>
  <div>
    <%= link_to book_path(book.id) do %>
    <%= image_tag book.get_image %>
    <p>ユーザー：<%= image_tag 'sample-author1.jpg' %></p>
    <p>題名：<%= post_image.shop_name %></p>
    <p>感想：<%= post_image.caption %></p>
    <p>ユーザーネーム：<%= post_image.user.name %></p>
  </div>
<% end %>