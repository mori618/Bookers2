<h1>画像投稿フォーム</h1>
<%= form_with model: @book do |f| %>
  <h4>表紙</h4>
  <%= f.file_field :image, accept: "image/*" %>
  <h4>題名</h4>
  <%= f.text_field :shop_name %>
  <h4>感想</h4>
  <%= f.text_area :caption %>
  <%= f.submit '投稿' %>
<% end %>